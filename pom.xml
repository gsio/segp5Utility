<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>ssena.pjt</groupId>
	<artifactId>beit-2026-segp5-qr</artifactId>
	<name>segp5-qr-was</name>
	<packaging>war</packaging>
	<version>1.0.0-BUILD-SNAPSHOT</version>

	<!-- ============================================================
	     [Phase 5 정리 — 2026.05.18]
	     운영 catalina.out 누수 경고(JDBC Driver / MySQL Statement Timer)
	     해결을 위한 Spring 5.x 마이그레이션 + 라이브러리 부채 정리.
	     변경 이력은 각 dependency 위 주석 참조.
	     인계 문서 §10에 별도 Phase로 분리된 항목(MyBatis/Swagger/log4j 등)은
	     본 Phase에서 손대지 않음.
	     ============================================================ -->

	<properties>
		<java-version>1.8</java-version>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<org.springframework-version>5.1.5.RELEASE</org.springframework-version>
		<org.aspectj-version>1.9.2</org.aspectj-version>
		<org.slf4j-version>1.7.25</org.slf4j-version>
		<spring.security.version>5.0.11.RELEASE</spring.security.version>
		<org.mybatis.version>3.1.1</org.mybatis.version>
		<org.mybatis.spring.version>1.1.1</org.mybatis.spring.version>
		<mysql.connector.version>8.0.33</mysql.connector.version>
		<jackson.version>2.9.10</jackson.version>
	</properties>

	<dependencies>
		<!-- ===== Spring Framework ===== -->
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
			<version>${org.springframework-version}</version>
			<exclusions>
				<exclusion>
					<groupId>commons-logging</groupId>
					<artifactId>commons-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-webmvc</artifactId>
			<version>${org.springframework-version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-web</artifactId>
			<version>${org.springframework-version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-orm</artifactId>
			<version>${org.springframework-version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context-support</artifactId>
			<version>${org.springframework-version}</version>
		</dependency>

		<!-- ===== Spring Security ===== -->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-core</artifactId>
			<version>${spring.security.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-config</artifactId>
			<version>${spring.security.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-web</artifactId>
			<version>${spring.security.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-taglibs</artifactId>
			<version>${spring.security.version}</version>
		</dependency>
		<!-- 
		  [Phase 5 변경 이력 — 2026.05.18]
		  crypto만 3.2.3.RELEASE로 다른 Security 모듈(5.0.11)과 버전 불일치.
		  Spring Security 5.x 내부 PasswordEncoder 인터페이스 호환성 깨질 가능성 있어
		  ${spring.security.version}으로 통일.
		-->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
			<version>${spring.security.version}</version>
		</dependency>

		<!-- ===== Bean Validation (Hibernate Validator) ===== -->
		<!-- 
		  [Phase 5 변경 이력 — 2026.05.18]
		  4.2.0.Final(Bean Validation 1.0/JSR-303)이 validation-api 1.1.0.Final(JSR-349)과
		  스펙 불일치로 Spring 5.1.5의 LocalValidatorFactoryBean 초기화 시 AbstractMethodError 발생.
		  Spring 5.1.x와 동시대 릴리즈인 6.0.x 계열로 통일.
		  groupId가 5.x부터 org.hibernate → org.hibernate.validator로 변경됨.
		  validation-api도 JSR-380(2.0)으로 함께 업그레이드 — Hibernate Validator 6.x가 이를 구현.
		-->
		<dependency>
			<groupId>org.hibernate.validator</groupId>
			<artifactId>hibernate-validator</artifactId>
			<version>6.0.22.Final</version>
		</dependency>
		<dependency>
			<groupId>javax.validation</groupId>
			<artifactId>validation-api</artifactId>
			<version>2.0.1.Final</version>
		</dependency>
		<!-- Hibernate Validator 6.x가 메시지 보간에 EL 사용. Tomcat 9 내장 EL 3.0과 매칭. -->
		<dependency>
			<groupId>org.glassfish</groupId>
			<artifactId>javax.el</artifactId>
			<version>3.0.1-b12</version>
		</dependency>

		<!-- ===== MyBatis (별도 Phase 검토 대상 — 인계 문서 §10 P3) ===== -->
		<dependency>
			<groupId>org.mybatis</groupId>
			<artifactId>mybatis</artifactId>
			<version>${org.mybatis.version}</version>
		</dependency>
		<dependency>
			<groupId>org.mybatis</groupId>
			<artifactId>mybatis-spring</artifactId>
			<version>${org.mybatis.spring.version}</version>
		</dependency>

		<!-- ===== Database ===== -->
		<!-- MySQL Driver — 운영 RDS MySQL 8.0.44 호환 -->
		<dependency>
			<groupId>mysql</groupId>
			<artifactId>mysql-connector-java</artifactId>
			<version>${mysql.connector.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-dbcp2</artifactId>
			<version>2.4.0</version>
		</dependency>

		<!-- ===== AOP ===== -->
		<dependency>
			<groupId>org.aspectj</groupId>
			<artifactId>aspectjrt</artifactId>
			<version>${org.aspectj-version}</version>
		</dependency>

		<!-- ===== Logging (log4j 1.x → 2.x 마이그레이션은 별도 Phase — 인계 문서 §10 P2) ===== -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${org.slf4j-version}</version>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>jcl-over-slf4j</artifactId>
			<version>${org.slf4j-version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-log4j12</artifactId>
			<version>${org.slf4j-version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>log4j</groupId>
			<artifactId>log4j</artifactId>
			<version>1.2.15</version>
			<exclusions>
				<exclusion>
					<groupId>javax.mail</groupId>
					<artifactId>mail</artifactId>
				</exclusion>
				<exclusion>
					<groupId>javax.jms</groupId>
					<artifactId>jms</artifactId>
				</exclusion>
				<exclusion>
					<groupId>com.sun.jdmk</groupId>
					<artifactId>jmxtools</artifactId>
				</exclusion>
				<exclusion>
					<groupId>com.sun.jmx</groupId>
					<artifactId>jmxri</artifactId>
				</exclusion>
			</exclusions>
			<scope>runtime</scope>
		</dependency>

		<!-- ===== JSON (Jackson) ===== -->
		<!-- 
		  [Phase 5 변경 이력 — 2026.05.18]
		  Jackson 2.4.4(2014)는 Spring 5.1.5 권장 버전(2.9.x)과 거리 큼.
		  CVE-2017-7525 등 다수 보안 취약점 존재. 2.9.10으로 통일.
		  jackson-databind만 2.9.10.8 (보안 패치 마지막 버전).
		-->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>2.9.10.8</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-core</artifactId>
			<version>${jackson.version}</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-annotations</artifactId>
			<version>${jackson.version}</version>
		</dependency>

		<!-- 
		  [Phase 5 변경 이력 — 2026.05.18]
		  org.json:json 20141113 → 20231013. JDK 1.8 호환 마지막 버전,
		  CVE-2022-45688(StackOverflow DoS) 해결.
		-->
		<dependency>
			<groupId>org.json</groupId>
			<artifactId>json</artifactId>
			<version>20231013</version>
		</dependency>

		<!-- json-simple (JsonReader.java 사용) -->
		<dependency>
			<groupId>com.googlecode.json-simple</groupId>
			<artifactId>json-simple</artifactId>
			<version>1.1.1</version>
		</dependency>

		<!-- 
		  JSONVIEW
		  [Phase 5 변경 이력 — 2026.05.18]
		  json-lib-ext-spring 1.0.2(2008년)가 transitive로 javax.servlet:servlet-api:2.4를 끌어와
		  pom에 명시한 javax.servlet:javax.servlet-api:4.0.1과 충돌 발생.
		  servlet-api 2.4의 HttpServletRequest는 getServletContext() 메서드가 없어서
		  PrintExcel.java 등에서 컴파일 에러. exclusion으로 차단.
		  json-lib-ext-spring 자체는 servlet-context.xml의 jsonView 빈에서 사용 중 (별도 Phase 정리 대상).
		-->
		<dependency>
		    <groupId>net.sf.json-lib</groupId>
		    <artifactId>json-lib-ext-spring</artifactId>
		    <version>1.0.2</version>
		    <exclusions>
		        <exclusion>
		            <groupId>javax.servlet</groupId>
		            <artifactId>servlet-api</artifactId>
		        </exclusion>
		    </exclusions>
		</dependency>

		<!-- ===== File Upload / IO ===== -->
		<!-- 
		  [Phase 5 변경 이력 — 2026.05.18]
		  commons-fileupload 1.2.2 → 1.4 (CVE-2016-1000031 해결).
		  commons-io 1.3.2(2007년) → 2.6 (JDK 1.8 호환, 1.x와 패키지 호환).
		-->
		<dependency>
			<groupId>commons-fileupload</groupId>
			<artifactId>commons-fileupload</artifactId>
			<version>1.4</version>
		</dependency>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>2.6</version>
		</dependency>

		<!-- ===== Excel (Apache POI) ===== -->
		<!-- 
		  [Phase 5 변경 이력 — 2026.05.18]
		  POI 4.1.2로 업그레이드 시도 → 기존 PrintExcel.java가 POI 3.x API 위에 광범위하게 작성되어
		  컴파일 에러 30+ 발생 (CellStyle 상수, HSSFColor.index, Font.BOLDWEIGHT, AnchorType 등).
		  본 Phase 본질(JDBC 누수)과 무관하고 마이그레이션 비용 크므로 3.11 유지.
		  POI 업그레이드는 인계 문서 §10 별도 Phase에 추가 — PrintExcel.java 전수 리팩터링 동반 필요.
		-->
		<dependency>
		    <groupId>org.apache.poi</groupId>
		    <artifactId>poi</artifactId>
		    <version>3.11</version>
		</dependency>
		<dependency>
		    <groupId>org.apache.poi</groupId>
		    <artifactId>poi-ooxml</artifactId>
		    <version>3.11</version>
		</dependency>

		<!-- ===== Servlet / JSP / JSTL ===== -->
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>javax.servlet-api</artifactId>
			<version>4.0.1</version>
			<scope>provided</scope>
		</dependency>
		<!-- 
		  [Phase 5 변경 이력 — 2026.05.18]
		  jsp-api 2.1 → javax.servlet.jsp-api 2.3.3. 
		  Tomcat 9는 JSP 2.3을 구현하므로 옛 jsp-api 2.1과 시그니처 불일치 가능성.
		  groupId/artifactId도 신규 표준으로 변경.
		-->
		<dependency>
			<groupId>javax.servlet.jsp</groupId>
			<artifactId>javax.servlet.jsp-api</artifactId>
			<version>2.3.3</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>jstl</artifactId>
			<version>1.2</version>
		</dependency>

		<!-- ===== Misc ===== -->
		<dependency>
			<groupId>javax.inject</groupId>
			<artifactId>javax.inject</artifactId>
			<version>1</version>
		</dependency>

		<!-- ===== Swagger (별도 Phase 정리 대상 — 인계 문서 §10 P3) ===== -->
		<!-- @ApiIgnore 어노테이션 9개 컨트롤러 사용 중 (Config 없음) -->
		<dependency>
			<groupId>io.springfox</groupId>
			<artifactId>springfox-swagger2</artifactId>
			<version>2.6.1</version>
		</dependency>
		<dependency>
			<groupId>io.springfox</groupId>
			<artifactId>springfox-swagger-ui</artifactId>
			<version>2.6.1</version>
		</dependency>

		<!-- ===== HttpClient ===== -->
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpclient</artifactId>
			<version>4.5.5</version>
		</dependency>

		<!-- ===== Mail ===== -->
		<dependency>
			<groupId>com.sun.mail</groupId>
			<artifactId>javax.mail</artifactId>
			<version>1.5.6</version>
		</dependency>

		<!-- ===== Image Metadata ===== -->
		<dependency>
			<groupId>com.drewnoakes</groupId>
			<artifactId>metadata-extractor</artifactId>
			<version>2.12.0</version>
		</dependency>

		<!-- ===== Test ===== -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.7</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-test</artifactId>
			<version>${org.springframework-version}</version>
			<scope>test</scope>
		</dependency>
		
		<!-- Sentry: 운영 예외 추적 (Phase 8) -->
		<!-- segp5 환경: JDK 1.8 + Spring 5.1.5 + javax 패키지 -->
		<!-- Sentry SDK 7.x는 JDK 17 요구, 따라서 6.x 마지막 안정 버전 사용 -->
		<dependency>
		    <groupId>io.sentry</groupId>
		    <artifactId>sentry-spring</artifactId>
		    <version>6.34.0</version>
		</dependency>
		
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<artifactId>maven-eclipse-plugin</artifactId>
				<version>2.9</version>
				<configuration>
					<additionalProjectnatures>
						<projectnature>org.springframework.ide.eclipse.core.springnature</projectnature>
					</additionalProjectnatures>
					<additionalBuildcommands>
						<buildcommand>org.springframework.ide.eclipse.core.springbuilder</buildcommand>
					</additionalBuildcommands>
					<downloadSources>true</downloadSources>
					<downloadJavadocs>true</downloadJavadocs>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.8.1</version>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
					<encoding>UTF-8</encoding>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>1.2.1</version>
				<configuration>
					<mainClass>org.test.int1.Main</mainClass>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>2.6</version>
				<configuration>
					<failOnMissingWebXml>false</failOnMissingWebXml>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>